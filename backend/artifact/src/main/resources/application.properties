# database connection
spring.datasource.url=jdbc:postgresql://${DATABASE_IP}:${DATABASE_PORT}/db
spring.datasource.username=fflernapp
spring.datasource.password=${DATABASE_PASSWORD}
# better performance and scalability (search Open Session In View)
spring.jpa.open-in-view=false
spring.datasource.driver-class-name=org.postgresql.Driver